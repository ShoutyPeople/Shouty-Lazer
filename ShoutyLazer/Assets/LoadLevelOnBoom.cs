using UnityEngine;
using System.Collections;

public class LoadLevelOnBoom : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AsyncLoad.NOW();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
